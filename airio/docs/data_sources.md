# Data Sources

## Data Sources

+   TensorFlow Datasets (TFDS) (`airio.TfdsDataSource`)
+   Files:
    +   ArrayRecord ([`airio.ArrayRecordDataSource`)
    +   Json (`airio.JsonDataSource`)
+   Functions (`airio.FunctionDataSource`)
